# python-everything_is_object

Exercises exploring Python's object model: object identity (`id`), object type (`type`), mutable vs immutable objects, aliasing, and how arguments are passed to functions.
